# Rust
/target
**/*.rs.bk

# Node / frontend
node_modules
/app/dist
/app/node_modules

# Playwright (D-3) — generated outputs
/app/test-results
/app/playwright-report
/app/blob-report
/app/.cache

# Tauri build output
/crates/termixion-tauri/target

# macOS
.DS_Store

# Secrets / signing (never commit — P0-2, trmx-48 updater key)
*.p12
*.p8
*.cer
*.mobileprovision
*.pem
*.key

# Editor
.idea
.vscode
*.swp

# Tauri generated schemas (regenerated by tauri-build on every build)
/crates/termixion-tauri/gen/schemas/
