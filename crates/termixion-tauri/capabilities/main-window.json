{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "main-window",
  "description": "Main-window-only grants (trmx-74): core:window:allow-close lets the tab manager close the window when the last tab closes. Scoped to main so the settings window gains no close-by-script surface it does not need.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:window:allow-close"
  ]
}
