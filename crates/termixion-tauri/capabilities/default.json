{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default capability set for the Termixion main window: PTY command/channel core permissions plus trmx-48 auto-update (updater/process), external-link opening (opener), and the open-settings menu event.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "updater:default",
    "process:default",
    "opener:default"
  ]
}
