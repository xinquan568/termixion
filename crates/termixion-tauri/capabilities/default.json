{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "default",
  "description": "Default capability set for the Termixion main window (A-1 skeleton; B-3/B-5 add the PTY command/channel permissions).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
